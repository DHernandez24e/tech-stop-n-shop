    <div class="navbar-nav mr-auto">
      <div class="nav-item active">
        <h1 class="nav-link text-center border-bottom">Profit Page</h1>
      </div>
      <div class="nav-item active">
        <h3 class="nav-link text-center" >Category Debt = (Items Sold)*(Item Cost) + (Number of Items in Inventory)*(Item Cost)</h3>
      </div>
      <div class="nav-item active">
        <h3 class="nav-link text-center">Category Income = (Items Sold)*(Item Price)</h3>
      </div>     
      <div class="nav-item active row">
        <h3 class="nav-link text-center row top-text border-bottom col-md-10" >Total Profit ($) = (Sum of Income in All Categories) - (Sum of Debt in All Categories) = </h3>
      {{#if profitFlag}}
        <h3 class="col-md-2 text-success">{{profitTotal}}</h3>
        {{else}}
      <h3 class="col-md-2 text-danger">{{profitTotal}}</h3>
      {{/if}}
      </div> 
