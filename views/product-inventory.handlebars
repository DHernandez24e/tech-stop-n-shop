    <div class="navbar-nav mr-auto">
      <div class="nav-item active">
        <h1 class="nav-link text-center border-bottom">Choose Product for Inventory Update</h1>
      </div>
      {{#each parsePost}}
      <div class="nav-item active">
        {{!-- <h3 class="nav-link text-center" >{{product_name}}</h3> --}}
       <a class="nav-link" href="products-update/{{id}}">{{product_name}}</a>        
      </div>
      {{/each}}
    </div>